@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Inter', sans-serif;
  background: #f4f7fb;
  color: #172033;
}

button,
input,
select {
  font-family: inherit;
}

button {
  cursor: pointer;
}

/* =========================
   UTILITIES
========================= */

.hidden {
  display: none !important;
}

.content-section {
  display: none;
}

.active-section {
  display: block;
}


/* =========================
   LOGIN
========================= */

.login-page {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background:
    radial-gradient(circle at 20% 20%, #dff8f0, transparent 30%),
    radial-gradient(circle at 80% 80%, #dceeff, transparent 30%),
    #f5f8fc;
}

.login-card {
  width: min(430px, 92%);
  background: white;
  padding: 42px;
  border-radius: 24px;
  box-shadow: 0 20px 60px rgba(20, 40, 80, .12);
  text-align: center;
}

.logo-container {
  margin-bottom: 18px;
}

.logo-placeholder {
  width: 70px;
  height: 70px;
  margin: auto;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 20px;
  background: linear-gradient(135deg, #00a884, #147dff);
  color: white;
  font-size: 34px;
}

/*
  ======================================
  INCLUIR LOGO AQUÍ

  Puedes reemplazar el div anterior por:

  <img src="URL-DE-TU-LOGO"
       class="real-logo">

  ======================================
*/

.login-card h1 {
  font-size: 30px;
  margin-bottom: 8px;
}

.login-subtitle {
  color: #718096;
  font-size: 14px;
  line-height: 1.6;
  margin-bottom: 30px;
}

.input-group {
  text-align: left;
  margin-bottom: 18px;
}

.input-group label {
  display: block;
  font-size: 13px;
  font-weight: 600;
  margin-bottom: 8px;
}

.input-group input {
  width: 100%;
  padding: 14px 16px;
  border: 1px solid #dce3ec;
  border-radius: 10px;
  outline: none;
  transition: .2s;
}

.input-group input:focus {
  border-color: #00a884;
  box-shadow: 0 0 0 3px rgba(0,168,132,.1);
}

.remember {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 12px;
  margin-bottom: 22px;
}

.remember a {
  color: #008f72;
  text-decoration: none;
}

.primary-btn {
  border: 0;
  padding: 13px 20px;
  background: linear-gradient(135deg, #00a884, #078fbb);
  color: white;
  border-radius: 10px;
  font-weight: 700;
  transition: .2s;
}

.primary-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(0,168,132,.2);
}

.login-card .primary-btn {
  width: 100%;
}

.demo {
  margin-top: 20px;
  color: #9aa5b5;
  font-size: 11px;
}


/* =========================
   SIDEBAR
========================= */

.dashboard-page {
  min-height: 100vh;
  display: flex;
}

.sidebar {
  width: 255px;
  min-height: 100vh;
  background: #102a43;
  color: white;
  position: fixed;
  left: 0;
  top: 0;
  display: flex;
  flex-direction: column;
  padding: 25px 16px;
}

.brand {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 5px 10px 30px;
}

.brand strong {
  display: block;
  font-size: 15px;
}

.brand small {
  color: #91a7bd;
  font-size: 10px;
}

.mini-logo {
  width: 42px;
  height: 42px;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 12px;
  background: linear-gradient(135deg, #00c897, #208cff);
  font-size: 20px;
}

nav {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.nav-item {
  border: 0;
  background: transparent;
  color: #b8c7d9;
  text-align: left;
  padding: 13px 14px;
  border-radius: 10px;
  display: flex;
  gap: 12px;
  align-items: center;
  font-size: 13px;
  transition: .2s;
}

.nav-item:hover,
.nav-item.active {
  background: rgba(255,255,255,.1);
  color: white;
}

.nav-item.active {
  box-shadow: inset 3px 0 #00c897;
}

.sidebar-bottom {
  margin-top: auto;
}

.user-mini {
  border-top: 1px solid rgba(255,255,255,.1);
  padding-top: 20px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.avatar {
  width: 36px;
  height: 36px;
  background: #00a884;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 11px;
  font-weight: 700;
}

.user-mini strong {
  display: block;
  font-size: 11px;
}

.user-mini small {
  color: #9eb1c5;
  font-size: 9px;
}

.logout-btn {
  border: 0;
  background: transparent;
  color: #9eb1c5;
  margin-top: 15px;
  font-size: 11px;
}


/* =========================
   MAIN
========================= */

.main-content {
  margin-left: 255px;
  width: calc(100% - 255px);
  min-height: 100vh;
}

.topbar {
  background: white;
  padding: 22px 35px;
  border-bottom: 1px solid #e8edf3;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.topbar h2 {
  font-size: 20px;
}

.topbar p {
  font-size: 12px;
  color: #8290a3;
  margin-top: 4px;
}

.top-actions {
  display: flex;
  gap: 18px;
  align-items: center;
}

.status {
  font-size: 11px;
  color: #66768a;
}

.status-dot {
  width: 7px;
  height: 7px;
  display: inline-block;
  border-radius: 50%;
  background: #16b37d;
  margin-right: 5px;
}

.notification {
  position: relative;
  background: #f2f6fa;
  border: 0;
  padding: 10px;
  border-radius: 10px;
}

.notification span {
  position: absolute;
  top: -4px;
  right: -4px;
  width: 16px;
  height: 16px;
  background: #ed5b5b;
  color: white;
  border-radius: 50%;
  font-size: 9px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.content-section {
  padding: 30px 35px;
}

.welcome {
  background: linear-gradient(120deg, #e8faf5, #e9f5ff);
  padding: 28px;
  border-radius: 18px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 25px;
}

.welcome h1 {
  font-size: 22px;
}

.welcome p {
  color: #637387;
  font-size: 13px;
  margin-top: 7px;
}


/* =========================
   METRICS
========================= */

.metrics {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
  margin-bottom: 20px;
}

.metric-card {
  background: white;
  border: 1px solid #e8edf3;
  border-radius: 15px;
  padding: 20px;
  display: flex;
  align-items: center;
  gap: 15px;
}

.metric-icon {
  width: 45px;
  height: 45px;
  border-radius: 12px;
  background: #e8f8f4;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
}

.metric-card span {
  display: block;
  font-size: 10px;
  color: #8a97a8;
}

.metric-card strong {
  display: block;
  font-size: 20px;
  margin: 5px 0;
}

.metric-card small {
  font-size: 9px;
  color: #8491a3;
}

.positive {
  color: #00a477 !important;
}


/* =========================
   PANELS
========================= */

.grid-2 {
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  gap: 18px;
  margin-bottom: 20px;
}

.panel {
  background: white;
  border: 1px solid #e8edf3;
  border-radius: 15px;
  padding: 22px;
}

.panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}

.panel-header h3 {
  font-size: 14px;
}

.panel-header span {
  display: block;
  color: #8b98a8;
  font-size: 10px;
  margin-top: 5px;
}

.panel-header select {
  border: 1px solid #e1e7ef;
  padding: 7px;
  border-radius: 7px;
  font-size: 10px;
}


/* =========================
   CHART
========================= */

.chart {
  height: 190px;
  display: flex;
  align-items: flex-end;
}

.chart-bars {
  height: 160px;
  width: 100%;
  display: flex;
  align-items: flex-end;
  justify-content: space-around;
  border-bottom: 1px solid #e5ebf1;
}

.bar {
  width: 9%;
  max-width: 45px;
  background: linear-gradient(to top, #00a884, #43d6b0);
  border-radius: 7px 7px 0 0;
  position: relative;
  min-height: 20px;
}

.bar span {
  position: absolute;
  bottom: -25px;
  left: 50%;
  transform: translateX(-50%);
  color: #8996a7;
  font-size: 9px;
}


/* =========================
   SYSTEM STATUS
========================= */

.system-status {
  display: flex;
  flex-direction: column;
}

.status-row {
  display: grid;
  grid-template-columns: 1fr 1fr auto;
  align-items: center;
  border-bottom: 1px solid #eef2f6;
  padding: 14px 0;
}

.status-row span {
  color: #718096;
  font-size: 11px;
}

.status-row strong {
  font-size: 13px;
}

.status-row em {
  font-style: normal;
  font-size: 9px;
  padding: 5px 8px;
  border-radius: 20px;
  background: #e8f8f1;
  color: #008866;
}

.text-btn {
  border: 0;
  background: transparent;
  color: #008f72;
  font-size: 11px;
  font-weight: 600;
}


/* =========================
   ALERTS
========================= */

.alert-list {
  display: flex;
  flex-direction: column;
}

.alert-item {
  display: grid;
  grid-template-columns: 35px 1fr auto;
  gap: 12px;
  padding: 15px 0;
  border-bottom: 1px solid #edf1f5;
}

.alert-item strong {
  font-size: 12px;
}

.alert-item p {
  color: #7c8999;
  font-size: 10px;
  margin-top: 4px;
}

.alert-item > span {
  color: #9aa5b3;
  font-size: 9px;
}

.alert-item.warning {
  color: #d68a00;
}

.alert-item.success {
  color: #00a477;
}


/* =========================
   SECTION INTRO
========================= */

.section-intro {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 25px;
}

.section-intro h1 {
  font-size: 24px;
}

.section-intro p {
  color: #78879a;
  font-size: 12px;
  margin-top: 6px;
}

.live {
  font-size: 9px;
  color: #00a477;
  font-weight: 700;
}

.live span {
  width: 7px;
  height: 7px;
  display: inline-block;
  background: #00b582;
  border-radius: 50%;
  margin-right: 5px;
}


/* =========================
   MONITOR
========================= */

.monitor-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
  margin-bottom: 20px;
}

.big-monitor {
  background: white;
  padding: 25px;
  border: 1px solid #e5ebf2;
  border-radius: 18px;
}

.big-monitor span {
  display: block;
  color: #778699;
  font-size: 12px;
}

.big-monitor strong {
  font-size: 42px;
  margin-top: 10px;
  display: inline-block;
}

.big-monitor small {
  font-size: 15px;
  color: #738298;
}

.big-monitor p {
  color: #8895a6;
  font-size: 10px;
  margin-top: 10px;
}

.gauge {
  height: 8px;
  background: #edf2f6;
  border-radius: 20px;
  margin-top: 18px;
  overflow: hidden;
}

.gauge-fill {
  height: 100%;
  background: linear-gradient(90deg, #00a884, #39c8a6);
  border-radius: inherit;
}

.educational-box {
  display: flex;
  gap: 20px;
  align-items: flex-start;
}

.education-icon {
  width: 55px;
  height: 55px;
  background: #fff5d8;
  border-radius: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 25px;
}

.educational-box h3 {
  margin-bottom: 8px;
}

.educational-box p {
  color: #6e7d90;
  font-size: 12px;
  line-height: 1.7;
  margin-bottom: 15px;
}

.secondary-btn {
  background: #eef8f5;
  color: #008b6d;
  border: 0;
  padding: 9px 13px;
  border-radius: 8px;
  font-size: 10px;
  font-weight: 600;
}


/* =========================
   EQUIPMENT
========================= */

.equipment-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 18px;
}

.equipment-card {
  background: white;
  padding: 22px;
  border: 1px solid #e4eaf1;
  border-radius: 16px;
}

.equipment-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.equipment-icon {
  font-size: 25px;
}

.online,
.offline {
  font-size: 9px;
  color: #00a477;
}

.offline {
  color: #d95353;
}

.equipment-card h3 {
  margin: 18px 0;
  font-size: 14px;
}

.equipment-power strong {
  display: block;
  font-size: 25px;
}

.equipment-power span {
  color: #8996a7;
  font-size: 10px;
}

.progress {
  height: 7px;
  background: #edf2f6;
  margin-top: 18px;
  border-radius: 20px;
  overflow: hidden;
}

.progress div {
  height: 100%;
  background: #00a884;
}

.equipment-card p {
  font-size: 9px;
  color: #8491a3;
  margin: 7px 0 15px;
}


/* =========================
   LEARNING
========================= */

.learning-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 18px;
}

.lesson-card {
  background: white;
  padding: 25px;
  border-radius: 17px;
  border: 1px solid #e5ebf2;
}

.lesson-icon {
  width: 50px;
  height: 50px;
  background: #e8f8f4;
  border-radius: 13px;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 23px;
}

.lesson-level {
  color: #008f72;
  font-size: 9px;
  font-weight: 700;
  margin-top: 18px;
}

.lesson-card h3 {
  margin-top: 7px;
  font-size: 16px;
}

.lesson-card p {
  color: #7a899a;
  font-size: 11px;
  line-height: 1.6;
  margin: 9px 0 15px;
}

.lesson-progress {
  height: 6px;
  background: #edf2f5;
  border-radius: 20px;
  overflow: hidden;
}

.lesson-progress div {
  height: 100%;
  background: #00a884;
}

.lesson-card > span {
  font-size: 9px;
  color: #8b98a7;
  display: block;
  margin: 6px 0 15px;
}


/* =========================
   QUIZ
========================= */

.quiz-container {
  max-width: 850px;
  margin: auto;
}

.quiz-header {
  display: flex;
  justify-content: space-between;
  margin-bottom: 25px;
}

.quiz-label {
  font-size: 9px;
  font-weight: 800;
  color: #00a884;
}

.quiz-header h1 {
  font-size: 24px;
  margin: 7px 0;
}

.quiz-header p {
  color: #7d8a9b;
  font-size: 12px;
}

.score {
  background: white;
  border: 1px solid #e4ebf1;
  border-radius: 13px;
  padding: 15px 25px;
  text-align: center;
}

.score span {
  display: block;
  font-size: 9px;
  color: #8491a2;
}

.score strong {
  font-size: 25px;
  color: #00a884;
}

.question-card {
  background: white;
  padding: 35px;
  border-radius: 18px;
  border: 1px solid #e3eaf1;
}

.question-number {
  font-size: 10px;
  color: #00a884;
  font-weight: 700;
  margin-bottom: 20px;
}

.question-card h2 {
  font-size: 20px;
  line-height: 1.5;
  margin-bottom: 25px;
}

.answers {
  display: grid;
  gap: 10px;
}

.answers button {
  padding: 15px;
  background: #f7f9fb;
  border: 1px solid #e2e8ef;
  border-radius: 10px;
  text-align: left;
  transition: .2s;
}

.answers button:hover {
  border-color: #00a884;
  background: #eefaf7;
}

.answers button.correct {
  background: #e3f8ef;
  border-color: #00a477;
}

.answers button.incorrect {
  background: #ffe9e9;
  border-color: #e05a5a;
}

.feedback {
  min-height: 40px;
  padding-top: 15px;
  font-size: 12px;
  font-weight: 600;
}

.next-btn {
  margin-top: 10px;
}


/* =========================
   ALERTS FULL
========================= */

.alerts-full {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.alert-card {
  background: white;
  border: 1px solid #e4eaf1;
  border-radius: 15px;
  padding: 22px;
  display: grid;
  grid-template-columns: 45px 1fr auto;
  gap: 15px;
  align-items: center;
}

.alert-symbol {
  font-size: 25px;
}

.alert-card h3 {
  font-size: 14px;
  margin: 5px 0;
}

.alert-card p {
  color: #7b8999;
  font-size: 11px;
}

.alert-card small {
  display: block;
  color: #9ba6b4;
  font-size: 9px;
  margin-top: 8px;
}

.priority {
  font-size: 8px;
  font-weight: 800;
}

.high-label {
  color: #df5151;
}

.medium-label {
  color: #d18a00;
}

.low-label {
  color: #00a477;
}


/* =========================
   RESPONSIVE
========================= */

@media (max-width: 1000px) {

  .metrics {
    grid-template-columns: repeat(2, 1fr);
  }

  .grid-2,
  .monitor-grid {
    grid-template-columns: 1fr;
  }

}

@media (max-width: 750px) {

  .sidebar {
    width: 70px;
    padding: 20px 8px;
  }

  .brand div:not(.mini-logo),
  .nav-item:not(.active)::after,
  .nav-item {
    font-size: 0;
  }

  .nav-item span {
    font-size: 18px;
  }

  .brand {
    justify-content: center;
  }

  .user-mini div:not(.avatar) {
    display: none;
  }

  .main-content {
    margin-left: 70px;
    width: calc(100% - 70px);
  }

  .content-section {
    padding: 20px;
  }

  .topbar {
    padding: 18px 20px;
  }

  .welcome {
    flex-direction: column;
    align-items: flex-start;
    gap: 15px;
  }

  .equipment-grid,
  .learning-grid {
    grid-template-columns: 1fr;
  }

  .metrics {
    grid-template-columns: 1fr;
  }

  .top-actions .status {
    display: none;
  }

}

@media (max-width: 500px) {

  .login-card {
    padding: 28px 20px;
  }

  .section-intro {
    align-items: flex-start;
    gap: 10px;
    flex-direction: column;
  }

  .quiz-header {
    flex-direction: column;
    gap: 15px;
  }

  .score {
    width: 120px;
  }

}